{
  "kind": "build_request",
  "title": "Add Invoice Number column to GST Excel templates (retry build)",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Add a new required GST invoice column header named \"INVOICE NUMBER\" to the shared template header list so that all three generated Excel templates (Sheet A, Sheet B, Sheet C) include it in the first row and template validation requires it.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6",
          "msg-10"
        ],
        "quotes": [
          "add invoice number laso in template",
          "try"
        ]
      },
      "acceptanceCriteria": [
        "Downloading each template (A/B/C) produces an .xlsx file whose first row contains an \"INVOICE NUMBER\" column header.",
        "Uploading an .xlsx missing the \"INVOICE NUMBER\" header fails validation with a \"Missing required columns\" error that includes \"INVOICE NUMBER\".",
        "Uploading an .xlsx that includes the \"INVOICE NUMBER\" header passes template header validation (all else equal)."
      ]
    }
  ],
  "constraints": [
    "Do not modify any files under frontend/src/hooks/useInternetIdentity.ts or frontend/src/hooks/useActor.ts (immutable paths).",
    "Keep the backend as a single Motoko actor; do not introduce additional backend services."
  ],
  "nonGoals": [
    "Do not change reconciliation logic beyond making the new column available via the existing key/compare column selection UX.",
    "Do not introduce new authentication mechanisms beyond Internet Identity."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}